/**
 * Student Experience Evolution Module Page Renderer
 */

export function renderStudentEvolutionPage() {
  return `
    <div id="mod-student-evolution" class="mod-pane">
      <div id="studentEvolutionContainer">
        <!-- Rendered dynamically by studentEvolutionView.js -->
      </div>
    </div>
  `;
}
