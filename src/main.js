/**
 * Garden Experience — Main Application Entry Point
 * Decomposed bootstrap module adhering to Clean SaaS Architecture.
 */

import { bootstrapApp } from './app/bootstrap/bootstrapApp.js';

bootstrapApp();
